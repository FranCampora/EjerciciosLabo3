#include "stdlib.h"
#include "stdio.h"
#include "funciones.h"
#include "archivos.h"
#include "def.h"

/*
int idNumeroAleatorio(int desde, int hasta){
	int num = 0;
	num =(rand()%(hasta-desde+1))+desde;
	return num;
}

void producirProducto(int i){
	char texto[50];/*esto se puede pasar en def.h o que lea por parametro o que lea hasta "end of file", igual para consumir
	int inRes = 0;

	/*SI USO SPRINTF, TENGO QUE LIMPIAR CON MEMST
	memset(texto, 0x00, sizeof(texto));
	sprintf(texto,"Producto-%02d\n", i);/*en el "texto" le doy un numero al producto de i, que cambia en el main. Osea le doy el contenido en el texto
	inRes = escribirArchivo(texto);/*escribe en el file el texto*
	if (inRes == TRUE){
		printf("\nprducto-%02d producido\n", i);
	}else{
		printf("HUBO UN ERROR  AL ESCRIBIR EL PRODUCTO-%02d EN EL FILE\n",i);
		return;
	}	
}

void consumirProducto(){
	char leido[50];/*esto se puede pasar en def.h
	int inRes = 0;
	
	inRes = leerArchivo(leido);
	if(inRes == TRUE){
		printf("\n CONSUMIDO EL PRODUCTO: %s\n",leido);
	}else{
		printf("\nHUBO UN ERROR AL LEER EL FIKE\n");
	}
}*/

int cargarInt(char *mensaje){
	int num = 0;
	printf("%s", mensaje);
	scanf("%d", &num);
	return num;
}

void cargarChar(char *mensaje, char *destino){
	printf("%s",mensaje);
	scanf("%s",destino);
}

